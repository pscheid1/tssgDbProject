export const environment = {
  production: true,

  // with the implementation of set-env.ts, .env, and env.js, the
  // following code is nolonger needed.

  // TSSGAPPURL: $ENV.TSSGAPPURL,
  // TSSGAPP_PORT: $ENV.TSSGAPP_PORT,
  // TSSGAPIURL: $ENV.TSSGAPIURL,
  // TSSGAPIPORT: $ENV.TSSGAPIPORT
};
