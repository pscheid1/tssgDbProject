#!/usr/bin/env bash
#scheduledb.install

. ../../cmd.env

. scheduledb.env
logsDirValidate

msg BEGIN "Clean up and Install the scheduledb services ..."

./mongo.install &
sleep 15
./backend.install &
sleep 15
./frontend.install &

# Wait for all subprocesses to finish
wait

# We are not going to use this but leaving it
# in as a comment for now.  Will modify or remove later.
# NODE create_backend_base_url_file.js
msg END "... done."
